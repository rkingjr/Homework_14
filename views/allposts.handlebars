<div class="row">
    <div class="col-auto">
        <h2>Hi, {{users.username}}! </h2>
    </div>
</div>
<br>
{{#if posts.length}}
<div class="card flex-column">
    {{#each posts as |posts}}
    {{> editItem posts}}
    {{/each}}
</div>
{{/if}}
